@model UdemyMVC5.ViewModel.RandomMovieViewModel.MoviesViewModel

@{
    ViewBag.Title = "index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Movies</h2>

<table class="table table-hover">
    @foreach (var mov in Model.Movies)
    {
        <tr><td>@mov.name</td></tr>
    }
    </table>
